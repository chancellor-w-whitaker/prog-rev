export const headerWrappingDef = {
  autoHeaderHeight: true, // Adjust Cell Height to Fit Wrapped Text
  wrapHeaderText: true, // Wrap Text
  initialWidth: 200, // Optional: Set a default size
  resizable: true, // Enable resizing
};
